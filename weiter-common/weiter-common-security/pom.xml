<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.zw.weiter</groupId>
        <artifactId>weiter-common</artifactId>
        <version>1.0.1</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>weiter-common-security</artifactId>

    <description>
        weiter-common-security安全模块
    </description>

    <dependencies>

        <!-- Spring Web -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
        </dependency>

        <!-- Weiter Api System -->
        <dependency>
            <groupId>com.zw.weiter</groupId>
            <artifactId>weiter-api-system</artifactId>
        </dependency>

        <!-- Weiter Common Redis-->
        <dependency>
            <groupId>com.zw.weiter</groupId>
            <artifactId>weiter-common-redis</artifactId>
        </dependency>

    </dependencies>

</project>
